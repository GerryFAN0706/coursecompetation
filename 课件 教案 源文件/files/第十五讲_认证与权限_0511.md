---
marp: true
size: 16:9
theme: am_blue
paginate: true
headingDivider: [2,3]
footer: \ *Java Web应用开发 (Java Web Application Development)* *计算机科学与技术学院*
style: |
  section.cover_d footer {
    left: 75%;
    bottom: 25px;
  }
  .ai-badge {
    display: inline-block;
    background: #e8f5e9;
    color: #2e7d32;
    border: 2px solid #2e7d32;
    border-radius: 20px;
    padding: 4px 16px;
    font-size: 18px;
    font-weight: bold;
    margin: 4px 0;
  }
  .ai-box {
    background: linear-gradient(135deg, #e8f5e9 0%, #f1f8e9 100%);
    border-left: 5px solid #2e7d32;
    border-radius: 8px;
    padding: 16px 20px;
    margin: 12px 0;
  }
---

<!-- _class: cover_d -->
<!-- _header: "" -->
<!-- _footer: ![h:100](logo-lan.png) -->
<!-- _paginate: "" -->

# 用户认证与权限控制

###### Authentication and Authorization

**课程名称**: Java Web 应用开发 | 第十五讲
**授课方式**: 双语教学 (Bilingual Teaching)
**课程性质**: 专业核心课程 | 4学分/64学时
**学期**: 2025年秋季

## 本讲概要

###### Course Outline

<!-- _class: cols2_ol_ci fglass toc_a -->
<!-- _footer: "" -->
<!-- _header: "CONTENTS" -->
<!-- _paginate: "" -->

- [问题导入：系统安全吗？](#4)
- [AI辅助分组诊断](#6)
- [认证与授权核心概念](#10)
- [课堂修复与验证](#16)
- [课堂总结](#19)

## 教学目标

###### Learning Objectives

<!-- _class: bq-blue -->

> **知识目标 (Knowledge Objectives)**
>
> - 区分认证和授权的概念与流程
>   *(Distinguish authentication and authorization)*
> - 掌握基于会话的登录态维护机制
>   *(Master session-based login state maintenance)*
> - 掌握RBAC基于角色的访问控制思路
>   *(Master RBAC role-based access control)*

## 教学目标

###### Learning Objectives

<!-- _class: bq-green -->

> **能力目标 (Ability Objectives)**
>
> - 能够实现登录、登出和会话管理
>   *(Be able to implement login, logout and session)*
> - 能够编写基于角色的权限过滤器
>   *(Be able to write role-based filter)*
> - <span class="ai-badge">AI辅助</span> 能使用AI工具进行代码审查并批判性评估建议
>   *(Be able to use AI for code review and critically evaluate suggestions)*

## 本讲AI应用环节

###### AI Application in This Lecture

<div class="ai-box">

**AI辅助教学设计 (AI-Assisted Teaching Design)**

**工具 Tools**: Cursor IDE / ChatGPT / Claude
**环节 Stage**: 分组代码审查（课堂第5-15分钟）
**方式 Method**: 学生使用结构化提示词 → AI诊断代码缺陷 → 学生判断采纳
**原则 Principle**: AI是助手，不是答案 *(AI assists, not replaces thinking)*

</div>

**本讲覆盖的AI教学情境：**
- 师生机协同教学 *(Teacher-Student-AI collaboration)*
- 多维智能评价反馈 *(Multi-dimensional intelligent feedback)*

## 第一部分：问题导入

###### Part I: Problem Introduction

<!-- _class: lastpage -->
<!-- _footer: "" -->
<!-- _paginate: "" -->

## 能登录 ≠ 能安全控制访问

###### Logging in ≠ Secure Access Control

**课程作业管理系统演示 (Live Demo)**:

**测试1**: 用 `admin / admin123`（管理员）登录
- 首页菜单显示"管理后台" ✓
- 点击进入后台管理页 ✓

**测试2**: 用 `tom / tom123`（普通用户）登录
- 首页菜单也显示"管理后台" ✗ ← **不应该出现！**
- 点击也能进入后台 ✗ ← **严重安全问题！**

**核心问题 (Core Question)**:

> **如果一个普通用户登录后也能进入管理后台，这个系统安全吗？**
> *Is this system secure if a regular user can access the admin panel?*

## 分组任务

###### Group Assignment

<span class="ai-badge">AI辅助环节</span>

**三组并行诊断 (Three Groups, Parallel Diagnosis)**:

| A组：认证组 | B组：授权组 | C组：视图一致性组 |
|:---|:---|:---|
| 审查 `LoginServlet` `LogoutServlet` | 审查 `RoleBasedAuthFilter` | 审查 `index.jsp` 菜单 |
| Session管理是否安全？ | 权限过滤是否按角色控制？ | 菜单显示与权限一致？ |

**AI使用规则 (AI Usage Rules)**:
1. 使用任务卡中的**结构化提示词**与AI对话
2. AI的输出是"候选意见"——**是否采纳由你判断**
3. 准备汇报：AI发现了什么？对应什么知识点？你采纳了哪个建议？

## AI提示词示例

###### AI Prompt Example

<span class="ai-badge">AI辅助环节</span>

**B组提示词 (Group B Prompt)**:

```
请审查这个Java Web角色权限过滤器，重点检查：
1. 是否只判断了"已登录"，而没有判断角色
2. 是否正确识别ADMIN用户
3. 普通用户是否仍可能访问/admin/*路径

请按以下格式输出：
- 问题位置
- 风险说明
- 对应知识点（Authorization / RBAC / Filter）
- 修改建议
- 建议验证方式
```

**要点**: 提示词要求AI**对照知识点**输出，而非直接给代码

## AI辅助分组诊断

###### AI-Assisted Group Diagnosis

<span class="ai-badge">AI辅助环节 · 10分钟</span>

<!-- _class: lastpage -->
<!-- _footer: "" -->
<!-- _paginate: "" -->

## 各组汇报

###### Group Reports

<span class="ai-badge">AI辅助环节</span>

| 汇报内容 | A组：认证 | B组：授权 | C组：视图 |
|:---|:---|:---|:---|
| **AI发现的问题** | LogoutServlet退出不彻底 `removeAttribute` ≠ `invalidate` | Filter只检查isLoggedIn 未检查角色 | 登录即显示"管理后台" 未按角色区分 |
| **对应知识点** | Session生命周期 Logout安全 | Authorization RBAC | View Rendering 前端≠安全 |
| **采纳的建议** | 改为`invalidate()` | 获取User对象 检查`getRole()` | 改为`user.role=='ADMIN'` 条件显示 |

**教师点评 (Teacher's Comment)**:

> AI发现了3个关键问题——但遗漏了**Session Fixation**风险。
> AI是有力的辅助工具，但不能替代安全专家的判断。

## 第二部分：核心概念

###### Part II: Core Concepts

<!-- _class: lastpage -->
<!-- _footer: "" -->
<!-- _paginate: "" -->

## 认证与授权

###### Authentication & Authorization

**两个不同的概念 (Two Different Concepts)**:

<!-- _class: cols-2 -->

<div class="ldiv">

**认证 (Authentication)**

**问题**: 你是谁？
*Who are you?*

**过程**:
- 用户提供凭证（用户名+密码）
- 系统验证身份
- 确认用户身份

**比喻**: 身份证检查
*ID verification*

</div>

<div class="rdiv">

**授权 (Authorization)**

**问题**: 你能做什么？
*What can you do?*

**过程**:
- 检查用户角色/权限
- 判断是否有访问权限
- 允许或拒绝访问

**比喻**: 门禁卡权限
*Access card permission*

</div>

**顺序**: 先认证，后授权 | **核心**: 登录成功 ≠ 拥有所有权限

## 密码安全

###### Password Security

**不要明文存储密码！** *Never store plaintext passwords!*

**密码散列 (Password Hashing)**:

```java
// 注册时：存储散列值
String hash = PasswordUtil.sha256(password);
user.setPasswordHash(hash);

// 登录时：比对散列值
if (PasswordUtil.matches(password, user.getPasswordHash())) {
    // 登录成功
}
```

**推荐算法**: BCrypt, Argon2 (带盐值)
*SHA-256用于演示原理，生产环境请使用BCrypt*

## 登录认证流程

###### Login Authentication Flow

**LoginServlet实现**:

```java
@WebServlet("/login")
public class LoginServlet extends HttpServlet {
    protected void doPost(HttpServletRequest req, HttpServletResponse resp)
            throws IOException, ServletException {
        String username = req.getParameter("username");
        String password = req.getParameter("password");

        User user = userService.login(username, password);
        if (user != null) {
            req.getSession().setAttribute("user", user); // 只存user对象
            resp.sendRedirect(req.getContextPath() + "/index.jsp");
        } else {
            req.setAttribute("error", "用户名或密码错误");
            req.getRequestDispatcher("/login.jsp").forward(req, resp);
        }
    }
}
```

## 登出与Session安全

###### Logout & Session Security

**安全登出 (Secure Logout)**:

```java
@WebServlet("/logout")
public class LogoutServlet extends HttpServlet {
    protected void doGet(HttpServletRequest req, HttpServletResponse resp)
            throws IOException {
        HttpSession session = req.getSession(false);
        if (session != null) {
            session.invalidate();  // 彻底销毁Session
        }
        resp.sendRedirect(req.getContextPath() + "/login.jsp");
    }
}
```

**关键区别 (Key Difference)**:
- `removeAttribute("user")` — 只删一个属性，Session仍存活
- `invalidate()` — **彻底销毁**整个Session，所有属性一并清除

## 权限过滤器（修复后）

###### Authorization Filter (Fixed)

**基于角色的Filter** *Role-based filter*

```java
@WebFilter("/admin/*")
public class RoleBasedAuthFilter implements Filter {
    public void doFilter(ServletRequest req, ServletResponse res, FilterChain chain)
            throws IOException, ServletException {
        HttpServletRequest request = (HttpServletRequest) req;
        HttpServletResponse response = (HttpServletResponse) res;
        HttpSession session = request.getSession(false);

        if (session == null) {
            response.sendRedirect(request.getContextPath() + "/login.jsp");
            return;
        }
        User user = (User) session.getAttribute("user");
        if (user != null && "ADMIN".equals(user.getRole())) {
            chain.doFilter(req, res);   // ADMIN放行
        } else if (user != null) {
            response.sendError(403);    // 非ADMIN → 403
        } else {
            response.sendRedirect(request.getContextPath() + "/login.jsp");
        }
    }
}
```

## JSP按角色显示菜单

###### Role-Based Menu Display

**动态菜单 (Dynamic Menu)**:

```jsp
<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>

<c:if test="${sessionScope.user != null}">
    <p>欢迎，${sessionScope.user.displayName}</p>
    <a href="${pageContext.request.contextPath}/logout">退出</a>
</c:if>

<c:if test="${sessionScope.user.role == 'ADMIN'}">
    <a href="${pageContext.request.contextPath}/admin/panel.jsp">管理后台</a>
</c:if>
```

**核心原则 (Core Principle)**:

> 前端隐藏菜单 = 用户体验优化（UX）
> 后端Filter拦截 = **真正的安全边界**
> **完整方案 = 前端隐藏 + 后端控制**

## 第三部分：修复与验证

###### Part III: Fix & Verify

<!-- _class: lastpage -->
<!-- _footer: "" -->
<!-- _paginate: "" -->

## 最小测试清单

###### Verification Checklist

| 编号 | 测试场景 | 操作 | 预期结果 |
|:---|:---|:---|:---|
| T1 | 未登录访问后台 | 直接访问 `/admin/panel` | 重定向到登录页 |
| T2 | 普通用户越权 | `tom/USER` 登录后访问 `/admin/panel` | 返回 **403** |
| T3 | 管理员正常访问 | `admin/ADMIN` 登录后访问 `/admin/panel` | 正常进入 |
| T4 | 退出后再访问 | 退出后再访问 `/admin/panel` | 重定向到登录页 |
| T5 | 菜单一致性 | 不同角色查看首页菜单 | 仅ADMIN显示"管理后台" |

**现场验证 (Live Verification)**:
- T1 ✓ → T2 ✓ → T3 ✓ → T4 ✓ → T5 ✓
- **系统从"能登录"变成了"能安全控制访问"！**

## 修复前后对比

###### Before vs After

| 组件 | 问题版 (Before) | 修复版 (After) |
|:---|:---|:---|
| **RoleBasedAuthFilter** | 只检查`isLoggedIn` | 检查`user.getRole()=="ADMIN"` |
| **index.jsp** | 登录即显示管理入口 | 仅ADMIN显示 |
| **LogoutServlet** | `removeAttribute` | `invalidate()` |
| **LoginServlet** | 冗余存储4个属性 | 只存`user`对象 |

**知识点映射 (Knowledge Mapping)**:
- 修复1 → **Authorization + RBAC + Filter**
- 修复2 → **View Rendering + Session**
- 修复3 → **Session Lifecycle + Logout**
- 修复4 → **Session Design + Data Consistency**

## 课程思政：制度与秩序

###### Ideological Education: System & Order

**RBAC与社会治理 (RBAC & Social Governance)**

**技术层面 (Technical)**:
- 认证：你是谁？*Who are you?*
- 授权：你能做什么？*What can you do?*
- 原则：最小权限、默认拒绝

**社会治理层面 (Social Governance)**:

> **制度先行，权责明晰 (System First, Clear Responsibilities)**
> 任何系统都需要规则和边界
> 用户分类、权限隔离，保障公平与安全
> *Every system needs rules and boundaries*

**依法治网 (Governing Cyberspace by Law)**:
- 技术人员的责任：保护用户信息，遵守法律法规
- 维护网络空间秩序，保护公民权益

## 第四部分：课堂总结

###### Part IV: Summary

<!-- _class: lastpage -->
<!-- _footer: "" -->
<!-- _paginate: "" -->

## 课堂总结

###### Summary

**今天的三个核心收获 (Three Key Takeaways)**:

**1. 认证回答"你是谁"，授权回答"你能做什么"**
- Authentication ≠ Authorization
- 登录成功 ≠ 拥有所有权限

**2. RBAC + Filter = 后端权限控制的基本骨架**
- Filter是安全的第一道防线
- 前端隐藏 + 后端控制 = 完整方案

**3.** <span class="ai-badge">AI辅助</span> **AI帮你发现问题，但理解和验证是你的责任**
- AI发现了3个关键问题，但遗漏了Session Fixation
- 批判性判断 > 盲目采纳

## 课后拓展任务

###### After-Class Tasks

**分层实践作业 (Tiered Assignments)**:

| 层级 | 任务 |
|:---|:---|
| **基础** | 将密码哈希从SHA-256升级为BCrypt |
| **进阶** | 增加MANAGER角色，实现三级权限 |
| **挑战** | 防固定会话攻击 + Session超时 + "记住我" |

**AI使用反思（必做）**:
1. AI帮你发现了什么？
2. 你采纳了哪些建议？拒绝了哪些？为什么？
3. AI在代码审查中的优势和局限分别是什么？

<!-- _class: lastpage -->
<!-- _footer: "" -->
<!-- _paginate: "" -->

###### 感谢聆听！(Thank You!)

<div class="icons">

- <i class="fa-solid fa-graduation-cap"></i>
  计算机科学与技术学院

- <i class="fa-solid fa-book-open"></i>
  Java Web 应用开发

</div>
