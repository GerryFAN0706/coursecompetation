# 第十四讲教学设计：数据库事务与连接池 (20分钟)

## 课程信息
- **课程名称**: Java Web应用开发 (Java Web Application Development)
- **教学方式**: 双语教学（英文授课，关键术语中英文对照）
- **教学主题**: 数据库事务与连接池（事务控制、批处理、Druid基础）
- **授课时长**: 20分钟
- **适用对象**: 已完成三层与DAO重构、具备JDBC编程基础的学生

---

## 一、教学目标

### 知识目标
1.  理解事务（Transaction）的ACID特性与常见隔离级别（Isolation Level: READ COMMITTED/REPEATABLE READ等）。
2.  掌握JDBC事务控制（Transaction Control）：`setAutoCommit(false)`、`commit()`、`rollback()`、`Savepoint`。
3.  掌握批处理（Batch Processing）`addBatch()/executeBatch()`的使用场景与注意事项。
4.  了解连接池（Connection Pool）的作用、基本参数与Druid的基础配置与使用方式。

### 能力目标
1.  能在Service层实现跨DAO的一致性操作（如“扣减库存 + 生成订单”）。
2.  能正确在异常情况下回滚并恢复连接状态，避免连接泄漏与脏连接。
3.  能引入Druid连接池并通过`DataSource`获取连接替代`DriverManager`。

### 素质目标
1.  建立“数据一致性优先”的工程意识，避免“部分成功”导致的业务错乱。
2.  强化性能与稳定性意识：连接是昂贵资源，应池化复用并监控。
3.  **[思政融合]** 以“诚信交易与契约精神”类比事务：要么全部达成要么全部撤销，维护公平与秩序。

---

## 二、学情分析

### 知识基础
- **已掌握**：三层架构（Controller-Service-DAO）的分层设计与实现、DAO模式的接口抽象与JDBC实现、包结构组织规范、Service层业务编排基础、JDBC基本操作（增删改查、PreparedStatement）
- **待建立**：事务（Transaction）的ACID特性与隔离级别概念、JDBC事务控制API（setAutoCommit/commit/rollback）的使用方法、批处理（Batch Processing）机制、连接池（Connection Pool）的原理与配置

### 能力水平
- 学生已能独立完成三层架构的代码组织，具备从Servlet到DAO的完整数据流编写能力
- 在前一讲的重构练习中，学生已理解"连接传递"的必要性，但对"多个DAO共用同一连接以保证事务一致性"的实际操作尚缺乏经验
- 部分学生在异常处理方面仍存在资源泄漏（如未在finally中关闭连接）的习惯性问题

### AI素养现状
- 经过前序课程的系统训练，学生已能熟练使用AI工具辅助代码编写、审查与调试
- 在架构层面，学生已初步体验过利用AI生成代码骨架并进行审查，但尚未将AI应用于概念辨析与技术方案决策场景
- 本讲将进一步引导学生利用AI工具（Claude）进行事务隔离级别的场景化分析——学生描述业务场景，AI给出隔离级别建议，学生批判性评估AI建议的合理性并通过实验验证

---

## 三、教学重点与难点

### 教学重点
-   Service层事务边界的定义与实现；统一连接的传递。
-   批处理在大批量场景的性能收益与参数控制。
-   Druid连接池的最小可用配置与获取连接方式。

### 教学难点
-   在多个DAO方法中复用同一连接；避免DAO内部私自开关连接。
-   异常处理：回滚、重置`autoCommit`、关闭资源的顺序与兜底。
-   隔离级别选择与并发异常（丢失更新/幻读）取舍。

---

## 四、教学内容与时间安排

### 第一部分：问题引入——“一半成功”的灾难 (4分钟)

#### 内容要点
1.  订单创建需同时“扣减库存 + 写入订单表”。任一失败都应全部撤销。
2.  普通JDBC若每个DAO独立开关连接，容易出现部分提交，导致数据不一致。
3.  引出事务边界与连接池。

#### 教学方法
-   场景叙述 + 失败案例剖析。

### 第二部分：新知讲解——事务控制与连接池 (10分钟)

#### 内容要点
1.  Service层事务模板：
    ```java
    public class OrderService {
        private final OrderDao orderDao = new JdbcOrderDao();
        private final InventoryDao inventoryDao = new JdbcInventoryDao();

        public void placeOrder(Long userId, Long productId, int qty) {
            Connection conn = null;
            try {
                conn = DataSourceUtil.getConnection(); // 来自连接池
                conn.setAutoCommit(false);              // 1) 开启事务
                conn.setTransactionIsolation(Connection.TRANSACTION_READ_COMMITTED);

                inventoryDao.decrease(conn, productId, qty);   // 2) 扣减库存
                Long orderId = orderDao.insert(conn, userId, productId, qty); // 3) 写订单

                conn.commit();                           // 4) 提交
            } catch (Exception e) {
                if (conn != null) try { conn.rollback(); } catch (SQLException ignore) {}
                throw new RuntimeException("下单失败，已回滚", e);
            } finally {
                if (conn != null) {
                    try { conn.setAutoCommit(true); } catch (SQLException ignore) {}
                    try { conn.close(); } catch (SQLException ignore) {}
                }
            }
        }
    }
    ```
2.  DAO层接受外部连接：
    ```java
    public class JdbcInventoryDao implements InventoryDao {
        @Override
        public void decrease(Connection conn, Long productId, int qty) throws SQLException {
            String sql = "UPDATE t_product SET stock = stock - ? WHERE id = ? AND stock >= ?";
            try (PreparedStatement ps = conn.prepareStatement(sql)) {
                ps.setInt(1, qty);
                ps.setLong(2, productId);
                ps.setInt(3, qty);
                if (ps.executeUpdate() == 0) throw new SQLException("库存不足");
            }
        }
    }

    public class JdbcOrderDao implements OrderDao {
        @Override
        public Long insert(Connection conn, Long userId, Long productId, int qty) throws SQLException {
            String sql = "INSERT INTO t_order(user_id, created_at) VALUES (?, now())";
            try (PreparedStatement ps = conn.prepareStatement(sql, Statement.RETURN_GENERATED_KEYS)) {
                ps.setLong(1, userId);
                ps.executeUpdate();
                try (ResultSet keys = ps.getGeneratedKeys()) {
                    if (keys.next()) {
                        long orderId = keys.getLong(1);
                        insertItemBatch(conn, orderId, productId, qty);
                        return orderId;
                    }
                }
            }
            throw new SQLException("插入订单失败");
        }
        private void insertItemBatch(Connection conn, long orderId, Long productId, int qty) throws SQLException {
            try (PreparedStatement ps = conn.prepareStatement(
                    "INSERT INTO t_order_item(order_id, product_id, qty) VALUES (?,?,?)")) {
                // 演示批处理：此处仅一行，实际可循环多行
                ps.setLong(1, orderId); ps.setLong(2, productId); ps.setInt(3, qty); ps.addBatch();
                ps.executeBatch();
            }
        }
    }
    ```
3.  Druid连接池最小可用配置（示例）：
    ```properties
    # druid.properties
    driverClassName=com.mysql.cj.jdbc.Driver
    url=jdbc:mysql://localhost:3306/demo?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username=root
    password=123456
    initialSize=5
    maxActive=20
    minIdle=5
    maxWait=60000
    ```
4.  获取连接的工具：
    ```java
    import com.alibaba.druid.pool.DruidDataSourceFactory;
    import javax.sql.DataSource;
    public class DataSourceUtil {
        private static DataSource ds;
        static {
            try {
                Properties p = new Properties();
                try (InputStream in = DataSourceUtil.class.getClassLoader().getResourceAsStream("druid.properties")) {
                    p.load(in);
                }
                ds = DruidDataSourceFactory.createDataSource(p);
            } catch (Exception e) {
                throw new ExceptionInInitializerError(e);
            }
        }
        public static Connection getConnection() throws SQLException { return ds.getConnection(); }
        public static DataSource getDataSource() { return ds; }
    }
    ```
5.  可选：保存点与回滚片段
    ```java
    Savepoint sp = conn.setSavepoint();
    // ... 执行部分操作
    conn.rollback(sp); // 回滚到保存点
    ```

#### 教学方法
-   代码走查 + 时序图（开启→执行→提交/回滚）+ 小实验（制造异常验证回滚）。

### 第三部分：实战——下单流程的一致性验证 (4分钟)

#### 内容要点
1.  故意在扣减库存后抛出异常，观察是否已成功回滚。
2.  增加批量插入订单项，观察批处理对性能的影响（以日志时间对比）。
3.  使用连接池并对比冷启动与复用连接的响应时间差。

#### 教学方法
-   现场联调 + 控制变量实验。

### 第四部分：课堂总结与展望 (2分钟)

#### 总结要点
1.  事务边界应在Service层，DAO不私自开关连接；异常要回滚并复位连接状态。
2.  连接池是生产标配；合理配置能显著提升吞吐与稳定性。

#### 展望（承上启下）
-   下一讲将把“安全与治理”向前推进：实现用户认证与基于角色的权限控制。

---

## 五、教学方法与手段

### 教学方法
1. **问题驱动 (Problem-Driven)**: 从"一半成功"灾难场景引入
2. **代码走查 (Code Walkthrough)**: 讲解Service层事务模板
3. **实验验证 (Experiment)**: 制造异常验证回滚机制
4. **对比实验 (Comparison)**: 批处理与连接池性能对比
5. **双语教学法 (Bilingual Teaching)**: 
   - 课堂采用英文授课，关键技术术语提供中英文对照
   - 重要概念（Transaction, ACID, Isolation Level, Batch Processing, Connection Pool）首次出现时给出英文表达和中文释义
   - 鼓励学生使用英文术语描述事务和连接池
   - 代码注释和变量命名使用规范英文

### 教学手段
-   IDE (IntelliJ IDEA/Eclipse)
-   Tomcat服务器 (Tomcat Server)
-   数据库客户端 (Database Client)
-   PPT/Marp课件 (Presentation Slides)

---

## 六、AI应用环节说明

### 6.1 使用工具

| 工具 | 使用者 | 场景 |
|:---|:---|:---|
| Claude（AI对话助手） | 教师 | 课堂演示：展示如何向AI描述业务场景并获取事务隔离级别建议 |
| Claude（AI对话助手） | 学生 | 概念辨析环节：描述特定业务场景，获取AI关于隔离级别选择与死锁风险的分析，并批判性评估 |

### 6.2 应用环节
AI辅助主要嵌入"第二部分：新知讲解——事务控制与连接池"的隔离级别讲解环节（约第8–12分钟）。在教师讲解完ACID特性与基本事务控制API后，利用Claude进行隔离级别的场景化分析：教师先演示一个业务场景的AI分析过程，随后学生自行设计业务场景向Claude提问，评估AI建议并在后续实验中验证。

### 6.3 设计目的
1. **化解抽象概念理解难题**：事务隔离级别（READ UNCOMMITTED/READ COMMITTED/REPEATABLE READ/SERIALIZABLE）是本讲最大难点，学生难以仅凭定义区分各级别的实际影响，AI的场景化分析可将抽象概念具象化
2. **培养技术方案决策能力**：学生不再被动接受"应该用哪个隔离级别"，而是主动描述场景、获取AI建议、评估建议合理性，建立"场景驱动决策"的工程思维
3. **训练对AI技术建议的批判性评估**：AI对隔离级别的建议可能忽略特定数据库引擎的实现差异（如MySQL InnoDB的间隙锁），学生需结合实际验证来判断AI建议的适用性

### 6.4 操作流程
```
第1步：教师向Claude描述业务场景——"电商系统中用户下单需同时扣减库存和生成订单，并发量约500 TPS，应选择哪种隔离级别？"
     ↓
第2步：展示Claude的分析结果（隔离级别推荐、死锁风险评估、性能影响），带领学生逐条评估AI建议的合理性
     ↓
第3步：学生分组设计不同业务场景（如银行转账、秒杀抢购、报表统计），向Claude提问并获取隔离级别建议
     ↓
第4步：学生对比AI建议与课堂所学理论，标记AI建议中的合理点与可能的局限性，在实验环节通过制造并发异常验证
```

### 6.5 预期效果

| 维度 | 传统课堂 | AI辅助课堂 |
|:---|:---|:---|
| 隔离级别理解 | 学生死记四种级别定义，难以与实际场景关联 | 通过场景化AI对话，学生理解每种级别的适用条件与权衡取舍 |
| 决策能力 | 教师直接给出"推荐READ COMMITTED"，学生被动接受 | 学生主动描述场景、获取AI建议、批判性评估，建立自主决策能力 |
| 风险意识 | 死锁与并发异常仅为理论概念 | AI分析具体场景下的死锁风险路径，学生对并发问题形成直观认知 |

### 6.6 防滥用措施
1. **任务限定**：Claude仅用于事务隔离级别的场景分析与风险评估，不用于直接生成事务控制代码（代码须学生手写）
2. **批判性评估**：学生必须在AI建议旁标注"同意/存疑/反对"及理由，不允许直接照搬AI结论
3. **教师审核**：教师审查学生与AI的对话记录，确保学生提出了有深度的业务场景而非简单问答
4. **AI使用边界**：AI是辅助者而非替代者，学生必须理解事务ACID特性与隔离级别原理后再参考AI输出，最终的隔离级别选择须通过实验验证

---

## 七、教学评价

### 过程性评价

| 评价维度 | 评价指标 | 评价方式 |
|:---|:---|:---|
| 知识理解 | 能否准确说出事务ACID四要素，并结合具体场景解释隔离级别的选用理由 | 课堂提问 |
| AI使用能力 | 能否合理使用Claude进行隔离级别场景分析，并对AI建议进行批判性评估，而非依赖AI直接获取答案 | 教师观察 |
| 风险分析能力 | 能否识别特定业务场景下的并发异常类型（脏读/不可重复读/幻读）与死锁风险 | 小组讨论 |
| 代码实践能力 | 能否解释批处理的性能优势与适用场景，并说明回滚时的注意事项 | 代码走查 |

### 终结性评价

| 评价维度 | 评价指标 |
|:---|:---|
| 事务实现能力 | 能否实现一个跨DAO的事务性操作（如"扣减库存+生成订单"），并在异常时正确回滚与资源释放 |
| 连接池配置能力 | 能否正确接入Druid连接池，通过`DataSource`获取连接，并合理配置基本参数 |

### AI辅助评价
- 教师可将学生的事务控制代码提交至AI工具，让AI检测是否存在常见问题（如未在finally中重置autoCommit、未正确处理回滚后的连接状态、资源泄漏等），作为辅助评分参考
- 收集学生与Claude的隔离级别分析对话，评估学生提问的场景设计质量与对AI建议的批判深度

---

## 八、课后拓展
### 思考题
1.  为什么要在`finally`中将`autoCommit`设回`true`？如果忘记，会出现什么隐患？
2.  生产环境如何选择隔离级别？面对高并发下幻读/丢失更新，你会如何权衡？

### 实践作业
1.  将“下单流程”抽象为事务模板方法，统一异常与回滚处理；
2.  在`druid.properties`中开启SQL监控，收集并分析慢SQL，给出优化建议。 