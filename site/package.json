{
  "name": "coursecompetation-site",
  "private": true,
  "version": "0.1.0",
  "description": "Competition website built with VitePress for GitHub Pages.",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "validate": "node scripts/check-frontmatter.mjs && node scripts/check-links.mjs",
    "generate:pdf": "node scripts/generate-pdfs.mjs"
  },
  "devDependencies": {
    "pdfkit": "^0.18.0",
    "vitepress": "^1.6.3"
  }
}
