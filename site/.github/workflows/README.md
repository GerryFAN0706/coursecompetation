# Workflow Location Note

GitHub Actions only executes workflows from the repository root at:

`.github/workflows/`

The active deployment workflow is:

`/.github/workflows/deploy-vitepress-pages.yml`

This folder exists to preserve the planned project layout documentation.
