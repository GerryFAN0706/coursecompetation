# 测试验证清单

> 使用说明：修改代码后，逐项完成以下验证。在"实际结果"列填写观察到的系统行为，在"通过"列标记 ✓ 或 ✗。

---

## 清单A：核心权限验证

| 编号 | 测试目标 | 测试步骤 | 预期结果 | 实际结果 | 通过 |
|:---|:---|:---|:---|:---|:---|
| T1 | 未登录拦截 | 不登录，直接在浏览器地址栏输入 `/admin/panel.jsp` | 被拦截，重定向到登录页 | | |
| T2 | 普通用户越权 | 用 `tom / tom123` 登录，直接访问 `/admin/panel.jsp` | 拒绝访问，返回 403 | | |
| T3 | 管理员正常访问 | 用 `admin / admin123` 登录，访问 `/admin/panel.jsp` | 正常进入后台 | | |
| T4 | 退出后拦截 | 登录后点击退出，再次访问 `/admin/panel.jsp` | 被拦截，重定向到登录页 | | |
| T5 | 登录失败反馈 | 输入错误密码 | 显示"用户名或密码错误" | | |
| T6 | 菜单一致性 | 分别以未登录/USER/ADMIN身份查看首页菜单 | 菜单显示与权限规则一致 | | |

---

## 清单B：视图与后端一致性

| 编号 | 测试目标 | 测试步骤 | 预期结果 | 实际结果 | 通过 |
|:---|:---|:---|:---|:---|:---|
| V1 | 未登录菜单 | 未登录状态打开首页 | 无"管理后台"入口 | | |
| V2 | 普通用户菜单 | 用 `tom / tom123` 登录，打开首页 | 无"管理后台"入口 | | |
| V3 | 管理员菜单 | 用 `admin / admin123` 登录，打开首页 | 显示"管理后台"入口 | | |
| V4 | 隐藏入口绕过 | 普通用户直接在地址栏输入 `/admin/panel.jsp` | 仍然拒绝访问 | | |

---

## 清单C：Session 行为验证

| 编号 | 测试目标 | 测试步骤 | 预期结果 | 实际结果 | 通过 |
|:---|:---|:---|:---|:---|:---|
| S1 | Session 建立 | 登录成功，刷新首页 | 页面仍显示用户信息 | | |
| S2 | Session 销毁 | 退出登录，刷新页面 | 不再显示已登录用户信息 | | |
| S3 | 角色权限生效 | 不同角色访问同一资源 | 表现出不同的权限行为 | | |

---

## 小组诊断记录

| 小组 | AI 发现的问题 | 小组判断是否有效 | 对应知识点 | 是否已修改 |
|:---|:---|:---|:---|:---|
| A组 | | | | |
| B组 | | | | |
| C组 | | | | |
