# 第五讲教学设计：Web应用的记忆核心—会话管理 (20分钟)

## 课程信息
- **课程名称**: Java Web应用开发 (Java Web Application Development)
- **教学方式**: 双语教学（英文授课，关键术语中英文对照）
- **教学主题**: Web应用的记忆核心：HttpSession会话管理
- **授课时长**: 20分钟
- **适用对象**: 已掌握Servlet和JSP基础，理解请求作用域（Request Scope）的学生

---

## 一、教学目标

### 知识目标
1.  理解HTTP协议的无状态性（Stateless）及其带来的挑战。
2.  掌握`HttpSession`对象的概念，理解其作为服务器端会话（Session）状态保持机制的原理。
3.  掌握获取`HttpSession`对象的方法：`request.getSession()`。
4.  掌握在`HttpSession`中存储和检索数据的方法：`setAttribute()`和`getAttribute()`。
5.  了解`JSESSIONID` Cookie在会话跟踪（Session Tracking）中的作用。

### 能力目标
1.  能够利用`HttpSession`实现跨多个请求的用户登录状态保持。
2.  能够区分`request`作用域和`session`作用域的生命周期和适用场景。
3.  能够编写简单的登录拦截逻辑，防止未授权用户访问受保护资源。

### 素质目标
1.  培养严谨的逻辑思维，理解Web应用中"状态"的重要性。
2.  建立安全意识，理解会话管理是Web安全的基础。
3.  **[思政融合]** 通过讲解HTTP的"无状态"和Session的"有状态"，类比个人与社会的关系。个人（请求）是独立的，但社会（应用）通过档案、身份等机制（Session）来记录和管理每个人的状态和历史，从而提供持续、稳定的服务。强调规则和契约（Cookie）在建立信任和维持秩序中的作用。

---

## 二、学情分析

### 知识基础
- **已掌握**：Servlet请求处理与响应机制、JSP页面渲染、EL表达式与JSTL标签的使用、请求作用域（Request Scope）的生命周期、请求转发与重定向的区别、MVC分层架构基本思想
- **待建立**：HTTP协议无状态性的深层理解、Session会话机制的创建与管理原理、Cookie在会话跟踪中的角色、Request作用域与Session作用域的本质区别、基于Session的访问控制与安全防护

### 能力水平
- 学生已能使用EL/JSTL编写"无脚本"的JSP视图页面，具备从Servlet到JSP的完整请求-响应-页面渲染链路的实践经验
- 具备一定的代码重构能力（从Scriptlet到EL/JSTL的重构经验）
- 对Web应用中"状态"概念的理解尚停留在单次请求层面，缺乏跨请求数据持久化的思维

### AI素养现状
- 学生已具备基本的AI工具使用经验（如ChatGPT问答、文本生成），能够撰写简单的描述性提示词向AI提问，但尚未形成结构化提示词（如分维度、分步骤提问）的习惯
- 在第四讲中已有使用AI进行EL/JSTL代码对比分析的实践经验，初步建立了"审查AI输出"的习惯；具体而言，学生能够识别AI生成代码中的基本语法错误，但对AI输出的安全性建议缺乏独立判断力
- 学生目前能够使用AI完成"代码解释""错误排查"等基础任务，但尚不具备使用AI进行多维度安全审计（如同时从认证、授权、数据保护角度分析代码）的能力
- 需引导学生从"AI代写"转向"AI辅助思考与验证"，本讲重点培养学生使用AI进行Session安全分析的能力，为后续JDBC安全审查奠定方法论基础

---

## 三、教学重点与难点

### 教学重点
-   HTTP无状态性的直观体现（刷新/跳转后数据丢失）。
-   `HttpSession`的核心作用：为每个用户在服务器端开辟一块"专属内存空间"。
-   `session.setAttribute()`和`session.getAttribute()`的用法，实现数据跨请求共享。
-   `request`作用域与`session`作用域的根本区别。

### 教学难点
-   **概念难点**: `HttpSession`的生命周期管理（何时创建、何时销毁）。
-   **原理难点**: 理解服务器是如何通过小小的`JSESSIONID` Cookie自动将浏览器的每次请求与服务器上对应的`HttpSession`对象关联起来的。这个过程对开发者是透明的，但理解它至关重要。

---

## 四、教学内容与时间安排

### **第一部分：健忘的HTTP - 特工身份的丢失 (3分钟)**

#### 内容要点
1.  **延续故事 (Continuing the Story):**
    *   "上一节课，我们的特工`007`已经成功登录，并用EL和JSTL看到了一个非常清爽的任务列表页面 (`mission.jsp`)。现在，他在任务页面点击了一个链接，想去查看他的个人档案 (`profile.jsp`)。"
2.  **制造问题 (Creating the Problem):**
    *   **展示代码**:
        *   `LoginServlet`: 将`agentInfo`对象存入`request`作用域，然后转发到`mission.jsp`。
        *   `ProfileServlet`: 一个新的Servlet，它尝试从`request`作用域获取`agentInfo`。
    *   **提问**: "当`ProfileServlet`执行时，它能成功拿到`agentInfo`对象吗？为什么？"
    *   **引导学生回答**: 不能。因为HTTP请求是"一次性"的。`LoginServlet`处理的请求和`ProfileServlet`处理的请求是两次完全不同的请求。前一个请求的`request`对象在响应结束后就已经销毁了。
3.  **点明本质 (Highlighting the Core Issue):**
    *   这就是 **HTTP协议的无状态性**。服务器默认是"健忘"的，它不记得上一个请求是谁发来的，干了什么。
    *   "这很糟糕！就像你每次去银行办事，都得重新提交全套身份证明一样。我们需要一个方法，让服务器'记住'我们的特工`007`。"

#### 教学方法
-   通过具体的用户场景和代码片段，让学生亲身体会"无状态"带来的问题。
-   设置悬念，激发学生寻找解决方案的兴趣。

### **第二部分：会话的诞生 - 服务器的记忆芯片 (8分钟)**

#### 内容要点
1.  **引入解决方案：`HttpSession`**
    *   **打个比方**: "为了解决健忘问题，服务器为每个首次到访的用户都分配一个'专属储物柜'，这就是`HttpSession`对象。用户在网站的整个访问期间，都可以使用这个柜子存取东西。"
2.  **如何获得储物柜：`request.getSession()`**
    *   **代码**: `HttpSession session = request.getSession();`
    *   **讲解**: 这行代码是`request.getSession(true)`的简写。当`true`作为参数时，服务器会检查浏览器发来的请求里有没有"储物柜钥匙"。
        *   **有钥匙**: 就用钥匙打开对应的柜子（返回已存在的Session）。
        *   **没钥匙**: 就创建一个全新的柜子（创建新Session），并把新配的钥匙交给浏览器。
    *   **重要变体**: `request.getSession(false)`
        *   **作用**: 只获取，不创建。如果根据"钥匙"找到了对应的柜子，就返回它；如果找不到，不会创建新的，而是直接返回`null`。
        *   **应用场景**: 这是实现**访问控制**的关键。在检查用户是否登录时，我们只关心他是否已经有会话，而不想为一个未经授权的访问创建新的会话。
3.  **储物柜的"钥匙"：`JSESSIONID`**
    *   **可视化**: 打开浏览器的开发者工具（F12 -> Application -> Cookies），展示那个名为`JSESSIONID`的Cookie。
    *   **讲解**: 这就是服务器发给浏览器的"钥匙"。浏览器很聪明，之后每次访问这个网站，都会自动带上这把钥匙。服务器看到钥匙，就知道该去哪个柜子里找东西了。这个过程是自动的！
    *   **技术补充：URL重写**
        *   **提问**: 如果浏览器禁用了Cookie怎么办？
        *   **解答**: 作为一种备选方案，服务器会将Session ID附加在URL的末尾，如 `.../profile.jsp;jsessionid=ABCDEF123456`。这种技术称为URL重写。虽然现在已不常用，但了解它有助于我们完整地理解会话跟踪机制。
4.  **使用储物柜：`setAttribute` 和 `getAttribute`**
    *   **存东西**: `session.setAttribute("user", agentInfo);`
    *   **取东西**: `AgentBean agent = (AgentBean) session.getAttribute("user");`
    *   **强调**: 这和`request`的操作方法完全一样，只是对象从`request`换成了`session`。

#### 教学方法
-   **比喻法**: "储物柜和钥匙"的比喻贯穿始终，帮助学生形象化理解抽象概念。
-   **工具演示**: 实际操作浏览器开发者工具，让学生眼见为实，增强信服力。

### **第三部分：实战 - 实现"一次登录，处处通行" (6分钟)**

#### 内容要点
1.  **改造`LoginServlet`**
    *   **Before**: `request.setAttribute("agentInfo", agent);`
    *   **After**:
        ```java
        // ...验证用户名密码成功后...
        HttpSession session = request.getSession(); // 获取或创建会话
        session.setAttribute("loggedInAgent", agent); // 将特工信息存入会话
        response.sendRedirect("mission.jsp"); // 重定向到任务页
        ```
2.  **创建/改造`ProfileServlet` -> 升级为 `AuthFilter` (更佳实践)**
    *   **引入过滤器 (Filter)**: 在企业级应用中，我们不应在每个Servlet中都写一遍登录检查逻辑。更好的方法是创建一个过滤器，集中处理所有受保护资源的访问控制。
    *   **关键逻辑**:
        ```java
        // AuthFilter.java
        public void doFilter(ServletRequest req, ServletResponse res, FilterChain chain) {
            HttpServletRequest request = (HttpServletRequest) req;
            HttpServletResponse response = (HttpServletResponse) res;
            HttpSession session = request.getSession(false); // 只获取，不创建

            if (session != null && session.getAttribute("loggedInAgent") != null) {
                // 用户已登录，放行
                chain.doFilter(req, res);
            } else {
                // 用户未登录，重定向到登录页
                response.sendRedirect(request.getContextPath() + "/login.jsp");
            }
        }
        ```
    *   **配置**: 在`web.xml`中配置此过滤器，使其拦截所有需要保护的路径（如 `/admin/*`）。

3.  **实现登出功能 (`LogoutServlet.java`)**
    *   将此功能从课后作业提升到课堂内容，形成闭环。
    *   **代码实现**:
        ```java
        @WebServlet("/logout")
        public class LogoutServlet extends HttpServlet {
            protected void doGet(HttpServletRequest request, HttpServletResponse response) throws IOException {
                HttpSession session = request.getSession(false);
                if (session != null) {
                    session.invalidate(); // 核心：销毁会话
                }
                response.sendRedirect("login.jsp"); // 重定向到登录页
            }
        }
        ```

4.  **JSP页面的变化**
    *   EL表达式 `${loggedInAgent.codename}` 依然可以工作！因为EL会自动从page, request, **session**, application中搜索对象。

#### 教学方法
-   **代码重构**: 现场修改代码，解决第一部分提出的问题，形成闭环。
-   **逻辑梳理**: 重点讲解`if-else`结构实现的登录状态判断和页面跳转逻辑。

### **第四部分：总结与展望 (3分钟)**

#### 总结要点
1.  **HTTP是无状态的**：这是Web工作的基本前提。
2.  **`HttpSession`是"解药"**：它在服务器端为每个用户维护一个状态，生命周期贯穿用户的整个访问过程（会话）。
3.  **`JSESSIONID`是"信使"**：它像一座桥梁，通过Cookie在客户端和服务器之间传递身份信息，关联起正确的会话。
4.  **作用域对比**:
    *   **Request**: 一次请求的生命周期。像一次"快递"，送完即止。
    *   **Session**: 一次会话的生命周期。像一个"随身背包"，只要用户不离开（或超时），东西就一直在。

#### **Session生命周期详解**
-   **创建**: 第一次调用`request.getSession()`或`request.getSession(true)`时。
-   **销毁**:
    1.  **手动销毁**: 调用`session.invalidate()`方法，通常用于"登出"功能。
    2.  **超时销毁**: 用户在指定时间内没有任何操作，服务器会自动销毁该Session。默认通常是30分钟。
    3.  **服务器关闭**: 应用停止或服务器关闭时。
-   **超时配置**: 可以在`web.xml`中统一配置所有Session的超时时间（单位：分钟）。
    ```xml
    <session-config>
        <session-timeout>20</session-timeout>
    </session-config>
    ```

#### 展望 (The Next Step)
-   "现在，服务器能记住我们的特工了。但特工的任务列表、个人信息、装备清单...这些数据都存在Session里。如果服务器重启，或者用户量巨大，Session会变得臃肿不堪，内存会爆炸。Session里的数据是**临时的**。"
-   "我们需要一个更可靠、更持久的地方来存储这些核心数据，一个不受服务器启停影响的'永久档案室'。"
-   "下一节课，我们将学习如何将数据存入真正的'保险库'——**数据库**，并学习一种名为**DAO（Data Access Object）**的设计模式来优雅地操作它。"

---

## 五、教学方法与手段

### 教学方法
1. **故事驱动法 (Story-Driven)**: 延续"特工007"故事线
2. **问题导向法 (Problem-Oriented)**: 从HTTP无状态问题入手
3. **比喻教学法 (Analogy)**: "储物柜与钥匙"比喻Session机制
4. **代码实战法 (Practice)**: 实现登录保持和访问控制
5. **双语教学法 (Bilingual Teaching)**: 
   - 课堂采用英文授课，关键技术术语提供中英文对照
   - 重要概念（Stateless, Session, Session Tracking, Cookie, Scope）首次出现时给出英文表达和中文释义
   - 鼓励学生使用英文术语描述会话管理机制
   - 代码注释和变量命名使用规范英文

### 教学手段
-   IDE (IntelliJ IDEA/Eclipse)
-   Tomcat服务器 (Tomcat Server)
-   浏览器开发者工具 (Browser DevTools)
-   PPT/Marp课件 (Presentation Slides)

---

## 六、AI应用环节说明

### 6.1 使用工具

| 工具 | 使用者 | 场景 |
|:---|:---|:---|
| Claude | 学生 | 将自己编写的Session管理代码提交给AI，让AI分析其中的安全隐患（Session固定攻击、Session劫持等） |
| Claude | 教师 | 演示阶段让AI生成包含典型Session安全漏洞的代码样本，作为反面教材 |

### 6.2 应用环节
AI主要应用于**第三部分"实战——实现一次登录处处通行"**的安全审查阶段（约第12-18分钟）。在学生理解Session的基本使用方法（setAttribute/getAttribute）后，引入AI工具对Session管理代码进行安全性审查，将安全意识教育从"知道有风险"提升到"能用工具发现风险"。

### 6.3 设计目的
1. **深化安全意识**：Session管理是Web安全的关键环节，传统课堂往往只提及"注意安全"却缺乏实践手段；借助AI模拟安全审查，让学生真切感受Session固定攻击（Session Fixation）和Session劫持（Session Hijacking）的威胁
2. **培养安全审查习惯**：引导学生养成"写完代码先做安全审查"的工程习惯，AI作为初步审查工具降低安全分析门槛
3. **对比学习**：通过AI同时展示"有漏洞的代码"和"安全的代码"，加深学生对`session.invalidate()`、`request.getSession(false)`等安全实践的理解

### 6.4 操作流程
```
第1步：学生完成LoginServlet中Session创建与用户信息存储的基础代码
     ↓
第2步：学生向Claude提交代码并使用结构化提示词进行安全审查（见下方示例）
     ↓
第3步：学生阅读AI输出的安全分析报告，理解每条风险的攻击原理和修复建议
     ↓
第4步：学生根据AI建议修改代码（如登录成功后调用session.invalidate()再创建新Session），并在IDE中验证修改后的代码功能正常
```

**示例提示词**: "审查以下Session管理代码，从会话固定攻击（Session Fixation）、超时配置合理性、敏感数据暴露三个维度分析安全隐患，并针对每个问题给出具体的修复代码。请用表格形式输出，列出：风险类型、风险等级（高/中/低）、问题描述、修复建议。"

```java
// 学生提交的待审查代码示例
HttpSession session = request.getSession();
session.setAttribute("loggedInAgent", agent);
session.setAttribute("password", agent.getPassword()); // 潜在安全隐患
response.sendRedirect("mission.jsp");
```

**提示词设计要点**：引导学生在提示词中明确指定审查维度（安全攻击类型）和输出格式（表格），培养结构化提问的习惯，避免笼统的"帮我检查代码"式提问。

### 6.5 预期效果

| 维度 | 传统课堂 | AI辅助课堂 | 可量化指标 |
|:---|:---|:---|:---|
| 安全认知深度 | 教师口头提及"Session有安全风险"，学生记忆模糊 | 学生亲手用AI发现自己代码中的具体漏洞，印象深刻 | 课后测验中安全知识题正确率预期从50%提升至80%以上 |
| 安全实践能力 | 学生知道概念但不会操作 | 学生掌握"编写→AI审查→修复→验证"的安全开发闭环 | 90%以上学生能在作业中主动实现Session重建（invalidate+recreate） |
| 代码质量意识 | 关注功能正确性 | 同时关注功能正确性和安全健壮性 | 提交代码中包含安全防护措施（如getSession(false)检查）的比例从20%提升至70% |
| AI工具使用效能 | 不涉及AI工具 | 学生能独立撰写多维度安全审查提示词 | 80%学生提交的提示词包含至少2个明确的审查维度 |

### 6.6 防滥用措施
1. **任务限定**：AI仅用于"Session管理代码安全审查"这一特定场景，不得用AI直接生成完整的登录/登出功能代码
2. **批判性评估**：要求学生对AI指出的每条安全风险写出自己的理解，判断AI的建议是否适用于当前项目场景（避免过度防御）
3. **教师审核**：教师预先准备包含典型漏洞的代码样本，引导学生使用AI分析后与标准答案对照，确保AI分析的准确性
4. **AI使用边界**：AI是辅助者而非替代者，学生必须理解Session的创建、销毁和跟踪原理后再参考AI的安全建议

---

## 七、教学评价

### 过程性评价

| 评价维度 | 评价指标 | 评价方式 |
|:---|:---|:---|
| 知识理解 | 能否区分`request.getSession()`和`request.getSession(false)`的行为差异；能否在浏览器DevTools中找到并解释`JSESSIONID` | 课堂提问 |
| AI使用能力 | 能否合理使用AI工具辅助分析Session管理代码的安全隐患，而非依赖AI直接获取答案 | 教师观察 |
| 安全分析能力 | 能否识别代码中Session固定攻击和Session劫持的风险点，并提出修复方案 | 安全审查练习 |
| 概念辨析 | 能否用"快递"和"背包"的比喻准确区分Request作用域与Session作用域 | 同伴互评 |

### 终结性评价

| 评价维度 | 评价指标 |
|:---|:---|
| 功能实现 | 能否独立编写基于Session的登录状态保持和AuthFilter访问控制逻辑 |
| 安全意识 | 能否在登录功能中主动实现Session重建（防止Session固定攻击） |
| 原理阐述 | 能否清晰地向他人解释JSESSIONID Cookie如何实现浏览器与服务器端Session对象的关联 |

### AI辅助评价

| 评价维度 | 评价指标 | 评价方式 |
|:---|:---|:---|
| 批量代码审查 | 教师使用AI工具批量扫描学生提交的Session管理代码，自动检测是否存在Session未销毁（缺少invalidate()调用）、敏感数据直接存入Session、未使用getSession(false)进行登录检查等安全缺陷 | AI自动扫描 + 教师复核 |
| 迭代改进能力 | 学生将修改后的安全代码再次提交给AI复查，对比第一次和第二次AI审查报告的差异，形成"编写→审查→修复→复查"的迭代改进闭环 | 学生提交两次AI审查对比记录 |
| 提示词质量 | 评估学生撰写的安全审查提示词是否包含明确的审查维度（至少2个）、指定的输出格式、具体的代码上下文，判断学生是否具备结构化使用AI的能力 | 教师审阅提示词记录 |

---

## 八、课后拓展
### 思考题
1.  `session.invalidate()` 和 `session.removeAttribute("user")` 有什么本质区别？
2.  如果两个不同的浏览器（如Chrome和Edge）同时访问同一个Web应用，它们会共享同一个Session吗？为什么？

### 实践作业
1.  **实现"记住我"功能**：研究Cookie的`setMaxAge()`方法，实现一个简单的"7天内免登录"功能。当用户勾选"记住我"并登录成功后，将一个包含用户身份标识的Cookie发送到客户端，并设置较长的有效期。
2.  **完善访问控制**: 完善`AuthFilter`，使其可以配置一些"白名单"路径（如登录页面`login.jsp`、CSS、JS等静态资源），这些路径即使用户未登录也可以直接访问。 